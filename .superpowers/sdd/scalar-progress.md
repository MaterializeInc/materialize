# Progress: scalar eqsat canonicalizer
Plan: .claude/plans/scalar_eqsat_canonicalizer.md
Design: doc/developer/design/20260625_eqsat_scalar_expressions.md
Branch: claude/mir-equality-optimizer-sodbej
Goal: non-destructive scalar canonicalizer (separate ScalarEGraph) replacing reduce in canonicalize_predicates; fixes CLU-137 bug class.

## ===== RESUME STATE (compaction 2026-06-27, CURRENT — trust this over older sections below) =====
WORKTREE: /home/moritz/dev/repos/materialize/.claude/worktrees/mir-equality-optimizer (git worktree; run everything here, do NOT cd to the main repo). HEAD = 18a1194cad, PUSHED to origin/claude/mir-equality-optimizer-sodbej. Working tree clean.
FLOW: subagent-driven-development (superpowers skill). Each task = a brief in .superpowers/sdd/, a fresh implementer subagent (opus for hard/soundness, sonnet for mechanical), then a reviewer subagent. Subagents run as named BACKGROUND agents; they send an idle_notification, then I retrieve their report (SendMessage asking them to reply, OR I tell them to WRITE the report to a file = more reliable). All briefs/reports/reviews are gitignored under .superpowers/sdd/ (.gitignore '*') so force-add them (git add -f).

STATUS: Phases 1, 2, 3 are IMPLEMENTED, REVIEWED, and CORPUS-VALIDATED. The flag enable_eqsat_scalar_canonicalize is DEFAULT TRUE on this branch (user direction: freestanding work, eqsat is the default here; production-elsewhere would be off). The canonicalizer is BEHAVIOR-NEUTRAL across the full local slt corpus with the flag on.
- Phase 1 (rules+analyses): const fold, AND/OR canonicalization (dedup/single/empty/short-circuit/drop-unit) + sort@raise + flatten_assoc, NOT/not-binary-negate/De Morgan, If true/false-null/same-branches/err-cond, null/err prop (could_error-gated), isnull_fold (non-nullable+could_error gated). All in src/transform/src/eqsat/scalar/rules.rs. const_fold SKIPS Panic (must never eval mz_panic at opt time).
- Phase 2 (CLU-137 split): factor_and_or (residual-error gated, full-intersection) + absorb_and_or (dropped-extra gated). Verified size-cost alone extracts the CLU-137 temporal form top-level (test_clu137_temporal_factors_to_top_level).
- Phase 3 (wiring): flag in feature_flags! (definitions.rs) + OptimizerFeatures (optimize.rs); mz-expr canonicalize_predicates gained canonicalize_predicates_with(.., Option<&dyn Fn>) (None==reduce, byte-identical); mz-transform bridge eqsat::scalar::canonicalize_predicates(preds,types,enable) injected at fusion::filter/predicate_pushdown/literal_constraints/fusion::join/normalize_ops/dataflow. Two ReduceScalars fixes (canonicalization.rs): elide emptied Filter + dedup converged predicates.
- Corpus validation (all 499 slt files, flag-on): result tests pass; only benign cosmetic EXPLAIN diffs (regenerated: literal_constraints, ldbc_bi_physical, ldbc_bi_eager_physical). cockroach+advent upstream: flag-on==flag-off byte-identical (zero impact). 2 real bugs caught+fixed (mz_panic crash; dup predicate).

INFRA for slt re-runs: CockroachDB v25.4.10 docker container `eqsat-crdb` running (host network, :26257). If gone, restart: `docker run -d --name eqsat-crdb --network host cockroachdb/cockroach:v25.4.10 start-single-node --insecure --listen-addr=localhost:26257`. Run slt: `COCKROACH_URL=postgres://root@localhost:26257 METADATA_BACKEND_URL=postgres://root@localhost:26257 bin/sqllogictest --optimized -- <files>` (flag is default-on; --rewrite-results to regen goldens; list-form is fast but has cross-file artifacts so confirm failures per-file). Build via mz-test skill conventions.

NEXT / OPEN:
- (Optional) final whole-branch review of Phases 2+3 (Phase 1 had one; 2+3 had per-task reviews only).
- Deferred eqsat rules (NONE are soundness issues; see reduce-vs-eqsat-rule-inventory.md): decompose_is_null, subset factoring, nullability e-class analysis (would let factor/absorb add the "operand non-nullable" gate disjunct), binary identity elim (x+0->x), Coalesce simplification, involution/inverse elim, If-with-boolean-branches->AND/OR, If-distribution.
- USER-CONFIRMED NEXT MAJOR EFFORT (after current phases): extract a SHARED GENERIC e-graph core unifying the relational (EqSatTransform) + scalar engines. This is the real "integration"; they are currently SEPARATE by design. Memory: eqsat-shared-core-extraction.
- Key docs: reduce-vs-eqsat-rule-inventory.md (gap map), the design note (CLU-137 + ENVELOPE soundness model: MZ has no single error semantics, exact-eval is a safe subset, CASE/If prescribed), memories mz-error-semantics-envelope / feature-flags-on-by-default-in-tests / scalar-eqsat-canonicalizer-status.
## ===== END CURRENT RESUME STATE =====

Goal(orig): flag enable_eqsat_scalar_canonicalize WAS default off; now default TRUE on this branch (see resume state).
## Phases (sequential; P0 round-trip gates all)
- Phase 0: scalar e-graph skeleton (node/egraph/lower/raise) + round-trip identity test. DONE (commit 09b1728df2, 9/9 round-trip tests pass, clippy+fmt clean). Reviewer APPROVED (no Critical/Important). Error-literal round-trip test gap fixed (commit 6d60d0f38a, 10/10 pass). PHASE 0 COMPLETE.
  Minors deferred to final whole-branch review: (1) raise.rs visiting:Vec->HashSet (O(depth^2) cycle guard, trivial Phase-1 prep); (2) canonicalize() omits col_types arg the plan specs -- intentional, Phase 1 adds it when lit-analysis needs it (signature change deferred to Phase 3 wiring); (3) add()/union() lazy-rebuild protocol (hashcons can hold stale non-canonical Id between union and rebuild; add() re-find()s so it is sound) -- doc-comment gap, same as relational engine. Bridge deviations from plan SNode spec (both needed to round-trip): Column carries name payload TreatAsEqual<Option<Arc<str>>>; Literal carries full Result<Row,EvalError> + ReprColumnType so error literals survive. Report: .superpowers/sdd/scalar-phase-0-report.md.

### VERIFIED PHASE 0 API (for Phase 1 briefs)
* SNode (node.rs): Column(usize, TreatAsEqual<Option<Arc<str>>>), Literal(Result<Row,EvalError>, ReprColumnType), CallUnmaterializable(UnmaterializableFunc), CallUnary{func,expr:Id}, CallBinary{func,expr1,expr2}, CallVariadic{func,exprs:Vec<Id>}, If{cond,then,els}. Helpers: map_children, children. Id = usize.
* ScalarEGraph (egraph.rs): add(SNode)->Id (hash-cons+canon), union(a,b)->bool, find(id)->Id, nodes(id)->Vec<SNode> (canonicalized class contents), rebuild() (congruence fixpoint), saturate()->usize. Bounds: MAX_ENODES=600, MAX_ITERS=100, MATCH_LIMIT=1000 (dead_code stub @egraph.rs:45, Phase 1 wires it). Rule insertion point @egraph.rs:204 (collect rewrites read-only, apply via union, set `changed`).
* lower(&mut ScalarEGraph,&MirScalarExpr)->Id (lower.rs); raise(&ScalarEGraph,Id)->MirScalarExpr min-cost DP w/ cycle guard (raise.rs).

### PHASE 1 DECOMPOSITION (SDD sub-tasks; ~40 rules too big for one implementer)
* 1a: analysis.rs -- e-class analyses (could_error: OR over class nodes, conservative; literal:Option). arity DROPPED (ill-defined as class analysis; rules read operand count from SNode). store HashMap<Id,ClassAnalysis> in egraph, maintained in add/union/rebuild. could_error mirrors scalar.rs:1226 at node level via func.could_error(). + unit tests. GATES 1b-1d. Brief: scalar-task-1a-brief.md. DONE + REVIEWED (commit 280d6432f1; reviewer a75129bc7b621f4f4 APPROVED). Important finding fixed inline (commit b1ccc8346c): union analysis fallback could_error=false was unsound direction -> now expect(); +restored Debug derive +empty-class debug_assert. 22/22 tests pass, clippy clean. Minor deferred to final review: find_in(uf,id) helper to dedup the inline find loop in add+rebuild. (First attempt ad5b94ea97f2440aa died on infra error, no trace; retry ac9a3332656c6fa53 succeeded.)
* 1b: rules.rs INFRA + constant folding ONLY (one rule proves infra end-to-end). Rule fn signature (read-only: &ScalarEGraph + Id + &SNode -> Vec<SNode> equivalents to union), wire match-collect/apply into saturate insertion point (egraph.rs:204) using MATCH_LIMIT + changed-tracking. Constant folding: fire when all operand classes are literals (analysis.literal Some); build MirScalarExpr from child literals; e.eval(&[], &RowArena) -> Result<Datum,EvalError>; wrap MirScalarExpr::literal(res, e.typ(&[]).scalar_type) -- EMPTY col_types is SOUND because a fully-literal subtree has no Column refs (this is why scalar canonicalize needs no col_types arg; confirms the reviewer's Minor is a non-issue for folding). Mechanism mirrors reduce/binary.rs:38-39. Per-rule property test (eval-equivalence incl nulls/errors). Brief: scalar-task-1b-brief.md. DONE_WITH_CONCERNS (commit 158e9db7a1, 30/30 pass, cargo check clean; rules.rs 282 lines + egraph.rs +77). Agent a8bd7c6a17b77ed29 (1st attempt died infra, retry succeeded). REVIEW IN FLIGHT (agent af4f0a42a180a5272, BASE 40f872b7bd HEAD 158e9db7a1).
  CONCERN (controller-confirmed pending reviewer): const_fold exposed a rebuild analysis-fixpoint bug. neg(0)->Literal(0) union makes CallUnary child canonicalize to the merged class itself (self-cycle). Agent's fix = skip unresolvable nodes, resolve class from ready (literal) node; once inserted, contains_key guard never revisits -> the cyclic node's could_error contribution is DROPPED. Gives could_error=false (semantic truth for neg(0), but VIOLATES the conservative-OR-over-nodes invariant; proper least-fixpoint gives true). SOUND only because const_fold cycles always contain a pinning literal; LATENT HAZARD for 1c/1d (non-literal unions could cycle w/o a literal -> under-approx could_error=false -> absorption guard enables unsound drop = CLU-137 class). Latent today (const_fold reads only .literal). REVIEWER CONFIRMED (af4f0a42a180a5272: Needs fixes, Important). FIXED (commit b204c25f3b): replaced skip-logic with seed-to-bottom monotone least-fixpoint in rebuild; cyclic child now reads class current value not dropped; +regression test test_fold_cycle_could_error_is_conservative asserts neg(0) cycle keeps could_error=true. 31/31 pass, clippy clean. 1B COMPLETE. Minor deferred to final review: class_ids() doc should note rebuild-precondition; (controller note) the seed-fixpoint is O(passes*nodes) but corpus small.
* 1c: boolean algebra + NOT (7 rules, contract-clean, NO infra change): and_or dedup/single/empty/short-circuit/drop-unit + not_not + not_binary_negate. Mirror reduce faithfully (scalar.rs:790 reduce_and_canonicalize_and_or; reduce.rs reduce_pre Not arm). Gate = per-rule property test diffing canonicalize vs eval over {true,false,null} cube + error operand (proves 3VL/short-circuit soundness). De Morgan DEFERRED (needs nested-node apply-step extension = own task). null/error propagation MOVED to 1d. Brief: scalar-task-1c-brief.md. DONE (commit 3b6777d33f, 37 pass). Controller added test_short_circuit_drops_nonliteral_error (commit c707bac127): closes the agent-flagged gap that the literal error test was collapsed by const_fold not the short-circuit rule; non-literal erroring operand (1/c0=5) forces the rule + differential incl c0=0 error row PROVES And short-circuits errors (CLU-137-class soundness verified). 38 pass, clippy clean. REVIEWED: APPROVED (agent ae4cf81572aa07d2e, no Critical). 2 Important = test-adequacy only (not soundness): drop-unit lacked non-literal/error operand; empty test vacuous. Fixed (commit 058a352ee6): added test_drop_unit_preserves_nonliteral_error (mirrors short-circuit fix), dedup Vec->HashSet (Minor), empty-test clarifying NOTE. 39 pass, clippy clean. 1C COMPLETE.
### REFINED SEQUENCING (two shared infra pieces gate the rest; build where first needed)
* INFRA-NESTED (needed by De Morgan AND Phase 2 factoring/absorption AND boolean-If->AND/OR): apply-step support for returning nested NEW nodes (recursive Build template + build() in egraph; migrate existing rules). Build alongside Phase 2 (its biggest consumer). Do NOT build twice.
* INFRA-COLTYPES (needed by null/error prop result types, If-Err-cond, Phase 2 consumer-catalog forms): thread col_types into canonicalize/ScalarEGraph (signature change reviewer flagged in 1b). Rules compute result types via raise(eg,id).typ(eg.col_types()). Build when first rule needs it.

### TASKS
* 1d (NEXT): if-condition resolution, TYPE-FREE cases only (contract-clean, no col_types, no nested nodes): if(true,a,b)->a; if(false|null,a,b)->b; if(c,x,x)->x (find(then)==find(els)). Source: reduce/if_then.rs reduce_if (cond.as_literal True/False/Null arms + then==els arm). Mirror faithfully. DEFER: Err-cond arm (needs type) + boolean-then/els If->AND/OR rewrites (need nested nodes + type). Per-rule eval-differential tests over cube (cond=literal so const_fold cannot fire; then/els=columns). Brief: scalar-task-1d-brief.md. DONE + REVIEWED APPROVED (commit 4fa23b58ba; reviewer aa9e74724b0aabb98, no Critical/Important). 2 cosmetic Minors fixed (commit d5c0d8d540): lit_bool_or_null doc contract-only, dropped "Rules 8-10" numbering. 43 pass. 1D COMPLETE.
USER DIRECTION (2026-06-27): finish Phase 1 rules first (1e null/error prop, then De Morgan), THEN infra+Phase2/3.
* 1e (DISPATCHED, agent a85a83f94897a8310, OPUS, BASE d5c0d8d540): INFRA-COLTYPES (canonicalize(expr,col_types); ScalarEGraph.with_col_types/col_types(); call_scalar_type helper via raise+typ) + null propagation binary+variadic. KEY: null-prop is could_error-GATED (deviation from reduce). PROBE established eval(AddInt64(null,1/0))==Err NOT null, so error wins; ungated null-prop would unsoundly change err->null and union null-vs-error classes. Gate: fire only when every OTHER operand could_error==false. No unary null-prop (subsumed by const_fold). Brief: scalar-task-1e-brief.md. Report->scalar-task-1e-report.md. DONE (commit 0ab00c4cf7, 47 pass, cargo check clean; E0061 LSP diag was stale). Variadic test uses VariadicFunc::MakeTimestamp (only readily-available propagates_nulls variadic; Coalesce/Greatest/Least are false). REVIEWED APPROVED (agent a18d90f2ebf3fa550, no Critical/Important; gate airtight, gate-proving tests verify non-firing + error-preservation, call_scalar_type + is_literal_null correct). 1E COMPLETE. Minors deferred to final review: (1) call_scalar_type clones raised children (Unary/Binary/If arms) - use into_iter; (2) redundant matches! before assert_eq in variadic fires-test; (3) null_prop_binary comment implies const_fold is exclusive handler on double-null but null-prop also fires (they agree, comment imprecise).
* 1f (DISPATCHED agent a851905b196430932, sonnet, BASE 0ab00c4cf7): error propagation binary+variadic, could_error-GATED (deviation from reduce, same as 1e: fire on literal-err operand only when every OTHER operand could_error==false; keeps exact-error soundness since eval is left-to-right and error identity isn't guaranteed). Binary no propagates_nulls gate (matches reduce); variadic keeps propagates_nulls gate. Reuses call_scalar_type; adds literal_err helper. Exact-error eval-differential tests incl blocked-by-1/c0-at-c0=0. Brief: scalar-task-1f-brief.md. Report->scalar-task-1f-report.md. DONE (commit a5c276a880, 52/52 pass, compiles clean; sits on ledger commit 0aa4eaeb13). literal_err helper + err_prop_binary (no propagates_nulls gate, matches reduce) + err_prop_variadic (propagates_nulls + could_error gate). REVIEWED APPROVED (agent a5f3ea65234c26758, no Critical; 1 Important = a WRONG comment claiming the gate blocks on multiple-error operands when it actually fires-with-first-error soundly; code verified sound). Comment fixed (commit dcf0d1e970). 1F COMPLETE. Minor (test comment c0==5) was actually accurate, left as-is.
* 1g (NEXT): If-Err-cond. if(err_cond, t, e) -> Literal(Err(cond_err), then_type.union(els_type)). UNCONDITIONALLY sound (eval evaluates cond first via ?, returns its error regardless of branches; NO could_error gate). Needs col_types (now available) for then/els full ReprColumnType + ColumnType::union. Skip if union returns None (conservative, avoid panic; reduce unwraps). Source: reduce/if_then.rs reduce_if Err arm. Brief: scalar-task-1g-brief.md. DONE + REVIEWED APPROVED (commit 0cb47bb2a5, 56 pass; reviewer ad95f4c418b9a5b90, no Critical; ReprColumnType::union returns Result<_,anyhow::Error>, conservative skip-on-Err instead of reduce unwrap). 1G COMPLETE. Findings deferred to FINAL whole-branch review (test-quality only, non-blocking): (Important) test_if_non_err_cond_not_affected uses if(c0,c1,c1) which if_same_branches consumes -> add if(c0,c1,c2) distinct-branch isolation test proving the If stays intact; (Minor) test_if_err_cond_branch_type_union should also assert err==DivisionByZero; (Minor) doc could cite reduce/if_then.rs path.
* REMAINING Phase 1 rule = De Morgan ONLY. Needs 1h infra first.
* 1h: PURE REFACTOR of rule contract fn(&mut ScalarEGraph,&SNode)->Vec<Id> so rules call eg.add for nested nodes + return ids to union. Migrated all 16 rules (vec![snode]->vec![eg.add(snode)]; eg.nodes(child)->vec![eg.find(child)]). Saturate: collect (id,node) snapshots read-only then apply with &mut rules. Brief: scalar-task-1h-infra-brief.md, report: scalar-task-1h-infra-report.md. DONE (commit ec01775d08, 56/56 tests pass UNCHANGED, clippy+fmt clean). REVIEWED APPROVED (agent review-1h, no Critical/Important/Minor; verified rule-by-rule behavior preservation incl not_not multi-match, saturate bounds/fixpoint identical, borrow discipline sound). 1H COMPLETE.
* 1i: De Morgan NOT(AND/OR exprs)->switched(map Not exprs) using 1h infra (eg.add the Not nodes + the switched variadic via VariadicFunc::switch_and_or, guarded by is_and_or). UNCONDITIONALLY sound, NO could_error gate. Do NOT port flatten_associative (sound on non-flattened variadic, saturation handles nesting). Brief: scalar-task-1i-brief.md, report: scalar-task-1i-report.md. DONE (commit 7d21177a7d, 59 scalar tests pass = 56 prior + 3 new; full mz-transform 257 pass/1 skip; clippy+fmt clean). Tests: cube soundness + firing-via-not_not-composition (canonicalize(NOT(AND(NOT c0,NOT c1)))==OR(c0,c1), needed because min-cost extraction prefers the cheaper original) + error-preservation over De-Morgan-extracted term (implementer corrected the is_err row to c1=false so AND does not short-circuit past the error). REVIEWED APPROVED (agent review-1i, OPUS, no Critical/Important). Reviewer independently re-derived ungated soundness against the real variadic.rs eval semantics (And/Or short-circuit MASKS recorded errors on false/true; otherwise surfaced error is order-independent std::cmp::max over ALL operand errors, variadic.rs:85/89/93/1159/1166) and found no counterexample over {T,F,N,E}; the De Morgan union cannot corrupt other extractions. 1I COMPLETE.
  Two cosmetic Minors deferred to FINAL whole-branch review (non-blocking, test/doc only): (1) test_not_demorgan_soundness is near-vacuous alone (extraction keeps the cheaper original so it compares input to itself) -- firing is proven by test_not_demorgan_fires_via_not_not; optional clarifying comment. (2) not_demorgan doc comment (rules.rs:52-54) attributes error-match to left-to-right eval; the precise reason is the order-independent max-error reduction; optional tightening.
* PHASE 1 RULES COMPLETE (1a-1i): could_error/literal analyses + const fold + boolean algebra/NOT + If-resolution + null/error prop + If-err-cond + De Morgan. De Morgan was the last Phase 1 rule.
* >>> CHECKPOINT USER before Phase 2 (undistribute/absorption = CLU-137 fix; reuses 1h nested-node infra) + Phase 3 wiring. <<< (reached 2026-06-27)
* PHASE 1 FIX BATCH DONE + REVIEWED APPROVED (impl fix-p1 OPUS commit 7957a3c277; reviewer review-fix-p1 OPUS, no Critical/Important). Report: scalar-phase1-fixes-report.md. Applied the 2 Important + lean Minors AND fixed TWO soundness bugs the new differential test surfaced:
  - BUG A: if_same_branches if(c,x,x)->x was UNSOUND w.r.t. condition errors (If evals cond first via ? at scalar.rs:1198, so an erroring cond surfaces even when branches identical; the rule dropped it). FIXED: gate on !could_error(cond), same philosophy as 1e/1f/if_err_cond. Deviation from reduce (reduce's then==els arm is ungated). Reviewer verified the gate stays sound under mid-saturation analysis staleness because could_error==false is VALUE-INVARIANT under union (union only joins eval-equal classes; make/union/rebuild only ever RAISE could_error).
  - BUG B: raise could PANIC mid-saturation: node_cost looked up child costs by raw (non-canonical) id (mid-saturation raise from if_err_cond/call_scalar_type runs between unions before rebuild -> stale ids), and the single-pass DFS was not a fixpoint (a class whose only finite derivation routes through a class on the DFS stack left uncosted -> build panic). FIXED: canonicalize child ids in node_cost + Bellman-Ford-style least-fixpoint relaxation, provably total (every class retains a grounded acyclic derivation under union) and terminating (positive-int costs, strictly decreasing). Subsumes the P0 raise cycle-guard Minor.
  - Other items: phase-level differential test test_canonicalize_eval_differential (deterministic seeded xorshift64, type-directed well-typed generator over c0/c1 Int64 + c2/c3 Bool, 3 seeds x 100 exprs x 225-row cube incl error rows 0/i64::MAX; value-based Result compare; THE soundness gate over rule interactions going forward, it found both bugs); find_in(uf,id) dedup of 3 union-find loops (egraph.rs); distinct-branch if(c0,c1,c2) if_err_cond non-fire test; merge literal-conflict debug_assert (analysis.rs); De Morgan doc tightened to max-error rationale. 61 scalar tests pass (59+2), nextest 259 pass.
  Two NEW Minors (non-blocking, perf/doc) deferred to a future review: (1) raise compute_costs clones nodes(rep) per pass + non-compressing find per child (bounded by MAX_ENODES=600; hoist if it shows in a profile); (2) generator could in theory differ run-to-run only if an expr hit MAX_ENODES/MATCH_LIMIT (cannot false-fail since all extracted forms are eval-equal; depth 2..=4 never approaches 600; optional comment).
* FINAL PHASE 1 REVIEW DONE (agent final-review-p1, OPUS; report: scalar-phase1-final-review-report.md). Verdict: WITH FIXES, NO Critical. Reviewer re-derived cross-rule saturation soundness against the real eval machinery (eager try_from_iter dispatch repr/scalar.rs:2134, LazyVariadicFunc enumeration, max-error reduction) -> no unsound rule interaction, no panic/non-termination. Two Important (both hardening, non-blocking): (I1) NO phase-level differential test canonicalize-vs-eval over a random corpus incl error rows -- add BEFORE Phase 3 wiring; (I2) extract find_in(uf,id) to dedup 3 inlined union-find loops (egraph.rs:137/269/~106). Fix-before-merge-lean Minors: 1g distinct-branch if(c0,c1,c2) non-fire test; (recommended) merge literal-conflict debug_assert (analysis.rs:111) + De Morgan doc max-error rationale (rules.rs). Branch pushed through 09999fbbf1.
* 1g (later): If-Err-cond (needs branch types via col_types now available).
* 1c-2 / Phase 2: INFRA-NESTED + De Morgan + factoring/absorption.
* Deferred/raise-time: canonical operand ordering (sort commutative operands at extraction, NOT a saturation rule; union-of-permutations explodes). RecordGet-on-RecordCreate (niche; include if cheap when scoped).
* 1d: if-condition resolution (reduce/if_then) + canonical operand ordering + RecordGet-on-RecordCreate + per-rule property tests.
* Source of truth = 54-rewrite inventory of MirScalarExpr::reduce (scalar.rs:689). 52 always-sound; ~6 metadata-inspecting (involution elim, if-then-else fn distribution) become rules w/ side conditions on could_error/preserves_uniqueness/type-unification.
* Phase 1 verify: differential test canonicalizer output observationally == reduce output across corpus.
- Phase 1: ~40 local rules + analyses (could_error/lit/arity) + per-rule property tests. COMPLETE (1a-1i + fix batch, all reviewed; final whole-phase review APPROVED with-fixes, fixes applied).
- Phase 2: undistribute factoring/absorption split + form-targeted extraction (consumer catalog). IN PROGRESS.
  DESIGN CORRECTION (workshopped with user 2026-06-27): factoring (a∧b)∨(a∧c)→a∧(b∨c) is NOT unconditionally sound under exact eval (restructuring moves err->null via short-circuit masking at factor=null even with nothing dropped; see design note + counterexample). Gate factoring on its RESIDUAL operands' could_error (NOT the whole expr, NOT the factor), absorption on the DROPPED operand. This narrows reduce's over-broad self.could_error() and fixes the real CLU-137 repro (fallible cast is in the common factor; residuals s IS NULL/s='' are error-free). Soundness is an ENVELOPE (no single MZ error semantics; non-strict AND/OR may reorder/elide errors; CASE/If prescribed); exact-eval is a safe subset, so conservative gates + the exact-eval differential test ship now; ungating later needs envelope formalized + refinement oracle (deferred). Design doc + plan amended (commit f4bd89bd7e). Memory: mz-error-semantics-envelope.
  * 2a (factoring, residual-error gated): full-intersection factoring, both duals, non-empty-residual condition keeps it DISTINCT from absorption. Brief scalar-task-2a-brief.md, report scalar-task-2a-report.md, review scalar-task-2a-review.md. DONE (commit bade736500, 66 scalar tests = 61+5; clippy+fmt clean; differential passes). REVIEWED APPROVED (review-2a OPUS, no Critical/Important). Reviewer worked the full value cube both duals incl factor=null/error: gate on residuals only correct; non-empty-residual separation sound; and the subtle residual=literal-true path (Or(And(a,true),And(a,c))->a) is STILL sound because c is gated error-free, so no UNGATED absorption can be produced. Deferred: subset factoring (reduce indexes_to_undistribute path); nullability gate disjunct (no nullability analysis). Two cosmetic Minors (factored_exprs not re-sorted; nodes() iteration-order comment) -> final Phase 2 review. 2A COMPLETE.
  * 2b (absorption, dropped-extra gated): a∨(a∧c)→a, a∧(a∨c)→a. Drop superset operand Q (inner-set(P)⊊inner-set(Q)); gate on could_error of the DROPPED EXTRAS (inner-set(Q)\inner-set(P)) only, retained P NOT gated (its error surfaces). Shared inner_sets helper with 2a. Brief scalar-task-2b-brief.md, report scalar-task-2b-report.md, review scalar-task-2b-review.md. DONE (commit 76dfa3beee, 70 scalar tests = 66+4; nextest 268 pass; clippy+fmt clean; differential exercises factoring+absorption together). One prior 2a test repurposed (test_factor_and_or_absorption_not_fired_here -> test_factor_hands_empty_residual_to_absorption): (c0∧c1)∨(c0∧c1∧c2) now absorbs to c0∧c1 once 2b exists, a correct consequence (reviewer confirmed not a weakening; factoring still declines empty residual, absorption collapses it soundly since c2 error-free). REVIEWED APPROVED (review-2b OPUS, no Critical/Important; both duals worked vs real eval incl a=null+dropped-extra-error and a=error, framing And/Or as join over total order {F,N,Err-by-max,T}). One optional Minor: canonicalize kept ids via eg.find before sort (redundant-node only, no soundness/termination) -> final Phase 2 review. 2B COMPLETE. >>> 2a+2b = the structural CLU-137 split is now implemented. <<<
  * 2c VERIFIED + DEFERRED (user decision 2026-06-27 "verify first, then design" -> "skip speculative 2c, go to Phase 3"). Verification (agent verify-clu137, commit 72c4530a95, test test_clu137_temporal_factors_to_top_level): under SIZE-COST ALONE the real CLU-137 predicate extracts to AND(c2,c3,c4, Lt(MzNow, CastTimestampTzToMzTimestamp(c0)), OR(IsNull(c1), Eq(c1,""))) -- temporal term is a flat top-level conjunct (14 nodes vs 22 DNF). So the rules (2a/2b)+size-cost CARRY the CLU-137 fix; no bonuses needed for the temporal consumer. Other consumers (literal-eq, filter-chars produced by upstream passes and preserved as binary comparisons; flat-conjunction came out flat) are non-regression concerns. Plan's bonus weights are "tuned against goldens" which only exist post-wiring, so the form-targeting cost objective is DEFERRED: add bonuses/rules ONLY where a Phase 3 golden regresses. (Possible concrete later item: associativity-flattening rule for flat conjunction, if nested Ands prove an issue.) 71 scalar tests pass.
- Phase 3: wire into CanonicalizeMfp (flag-gated reduce replacement) + full slt/td. IN PROGRESS.
  WIRING ARCHITECTURE FINDING (2026-06-27): the plan's "swap reduce at src/expr/src/relation/canonicalize.rs:230/437/87" is INFEASIBLE for production code: those reduce calls are in mz-expr's canonicalize_predicates (canonicalize.rs:216), and mz-transform (home of the eqsat scalar canonicalizer @scalar.rs:35 `pub fn canonicalize(&MirScalarExpr,&[ReprColumnType])->MirScalarExpr`) is only a [dev-dependencies] entry of mz-expr (Cargo.toml:75/83), so mz-expr cannot call it. Wiring must be at the mz-transform layer. CanonicalizeMfp (src/transform/src/canonicalize_mfp.rs) reaches canonicalize_predicates via fusion::filter. Candidate approach: inject an optional scalar-canonicalizer callback into canonicalize_predicates (default = MirScalarExpr::reduce), supplied by mz-transform callers when the flag is on. Flag enable_eqsat_scalar_canonicalize default off (mirror optimize.rs:155). USER DIRECTION (2026-06-27): wire the flag ON BY DEFAULT IN TESTS/CI (production default off) to get coverage of the new path against slt/td/goldens (memory feature-flags-on-by-default-in-tests; find the mechanism other recent flags use). WIRING DECIDED (user 2026-06-27): inject an optional scalar-canonicalizer callback into mz-expr canonicalize_predicates (default = MirScalarExpr::reduce), supplied by mz-transform callers (fusion::filter @filter.rs:96, predicate_pushdown @846) when the flag is on; FLAT-CONJUNCTION via a dedicated associativity-flattening eqsat rule (task 3a, in flight). Phase 3 sub-tasks: 3a flatten rule + termination hardening = DONE; 3b flag + callback wiring = DONE; 3c full slt/td/goldens with flag on (NEXT), address regressions (where deferred form-targeting may resurface).
  * 3b (flag-gated wiring). Flag enable_eqsat_scalar_canonicalize (default:false production OFF; auto test-ON via enable_all_feature_flags_by_default -> --all-features slt/td harness, adapter/catalog/open.rs:129). OptimizerFeatures field + bound at all sites (mirrors enable_eqsat_optimizer; exhaustive-destructure test guards no field dropped). mz-expr canonicalize_predicates = thin wrapper over canonicalize_predicates_with(.., scalar_canon: Option<&dyn Fn>); None == today's reduce EXACTLY (mz-expr gains only an optional param, no mz-transform dep). Single bridge eqsat::scalar::canonicalize_predicates(preds,types,enable). Wired call sites (flag from ctx.features): CanonicalizeMfp->rebuild_mfp->fusion::filter Filter::action (CLU-137 path), predicate_pushdown, literal_constraints, fusion::join, normalize_ops, global dataflow filter pushdown; eqsat/raise rebuild_mfp passes false (already saturated). Brief -3b-brief.md, report -3b-report.md, review -3b-review.md. DONE (commit af9dc879a5). REVIEWED APPROVED (review-3b OPUS, no Critical/Important): flag-OFF byte-identical across all 16 files (351 mz-transform+mz-expr tests pass flag-off, NO goldens regenerated), all bindings covered, callback no aliasing bug, broader wiring all flag-off-safe. Two Minor test-strength notes (clu137 flag-on test would also pass flag-off since reduce factors that DNF too -> it is a flag-on smoke test; genuine routing guard is the mz-expr callback test; the real could_error reduce-vs-eqsat differential is end-to-end only in 3c) -> address in 3c. 3B COMPLETE.
  * 3c IN PROGRESS. SURVEY FINDING: the mz-transform test_transforms datadriven goldens do NOT exercise canonicalize_predicates (forced flag on in the harness, ran REWRITE, NO goldens changed; temp stderr marker in the bridge fired 0 times). Only fusion goldens are join/top_k. So the real golden surface is sqllogictest EXPLAIN plans run with the flag on (== the --all-features path CI runs). Reverted all temp survey changes.
    - 3c-ordering (canonical AND/OR operand ordering at extraction): raise.rs sorts And/Or operands by MirScalarExpr::Ord (mirrors reduce exprs.sort()@scalar.rs:805), And/Or ONLY, exact-eval safe (order-independent join), also removes HashSet-iteration nondeterminism. NEEDED so EXPLAIN goldens differ from reduce only on genuine plan changes, not operand-order noise. Brief scalar-task-3c-ordering-brief.md, report -3c-ordering-report.md. DONE (commit af34355306, 282 tests pass, differential passes). Updated 2 existing tests to sorted order (test_not_demorgan_error_preservation OR(d,c1)->OR(c1,d); round_trip_nested_mix input pre-sorted since raise now canonicalizes order so identity holds only for canonical inputs). Verified diff directly (sort is And/Or-only, sound).
    - 3c slt EXPLAIN survey DONE (local, cockroach v25.4.10 in docker --network host on :26257; COCKROACH_URL/METADATA_BACKEND_URL=postgres://root@localhost:26257; method: copy slt, prepend `simple conn=mz_system` ALTER SYSTEM SET enable_eqsat_scalar_canonicalize=true, bin/sqllogictest --rewrite-results on the copy, diff vs committed golden). pushdown.slt: 0 diff (identical, ordering fix works). optimized_plan_as_text.slt: REGRESSION found (58-line diff, 4 occurrences, all identical pattern): flag-ON leaves an EMPTY-predicate Filter wrapping a differential Join that reduce eliminates. Triggering queries are TRIVIAL equi-joins (SELECT * FROM t,u WHERE t.b=u.c). KEY: canonicalize_predicates returns an IDENTICAL list for a plain Eq (eqsat does not rewrite it), so the empty Filter is NOT from predicate content. Filter::action removes empty Filters (filter.rs:107), so the shell is created AFTER the last fusion::filter pass -> almost certainly one of the BROADER-THAN-SCOPED wired sites (fusion::join @join.rs:71, dataflow global filter pushdown, predicate_pushdown) leaves it. NEEDS root-cause + fix before the flag is trustworthy. Likely fix: narrow the wiring to the predicate-canonicalization sites that actually need it (fusion::filter primary path), or ensure empty Filters from the join-path wiring are elided. Cockroach container `eqsat-crdb` left running for re-validation.
    - 3c-fix (empty-Filter regression): ROOT CAUSE = eqsat canonicalizer has NO isnull/nullability fold, so a redundant top-level IS NOT NULL (made redundant by a join equivalence on an indexed input) is not folded during canonicalize_mfp the way reduce folds it (reduce uses column nullability). The predicate reaches ReduceScalars whose whole-tree ReprRelationType analysis folds it to true and drops it, but ReduceScalars did not elide the emptied Filter -> stranded no-op shell. Only appears in the GLOBAL/dataflow stage with an indexed join input (LOCALLY OPTIMIZED is clean; single-view harness never reproduces -> why the datadriven goldens missed it). FIX (commit 06741f1419): ReduceScalars Filter arm elides the Filter once reducing empties its predicate list (general improvement, not flag-gated, no-op for flag-off since flag-off folds+removes it earlier). Brief in the fix dispatch; report scalar-task-3c-fix-report.md, review scalar-task-3c-fix-review.md. REVIEWED APPROVED (review-3c-fix, no Critical/Important; view navigation provably correct for Filter's one-child structure, flag-off byte-identical empirically 838/838 explain slt + 283 mz-transform + 74 mz-expr unchanged, CLU-137 intact, new test elides_filter_emptied_by_reduction). VERIFIED flag-on: optimized_plan_as_text.slt + pushdown.slt now clean. One cosmetic Minor (test doc narrates body). 3C-FIX COMPLETE.
    - 3d (IsNull fold, CLOSES the capability gap): IsNull(x)->false when x non-nullable AND could_error==false (gate is a DELIBERATE deviation from reduce's ungated fold; IsNull propagates operand errors so exact-eval needs it; consistent with 1e/1f). Nullability via raise(x).typ(col_types).nullable (mirrors reduce, no new analysis). Brief scalar-task-3d-isnull-brief.md, report -3d-isnull-report.md, review -3d-review.md. DONE (commit 01b0b02aca, 87 scalar tests = 83+4, differential passes). REVIEWED APPROVED (review-3d, NO issues at all; both gates correct on the operand, sound, tests non-vacuous incl the gate-block is_err proof). Now eqsat folds the IS NOT NULL itself (the regression's root); the ReduceScalars empty-Filter elision (06741f1419) stays as a correct general safety net. DEFERRED: decompose_is_null (reduce's nullable->disjunction-of-IsNulls). 3D COMPLETE.
    - USER DIRECTION (2026-06-27): no PR (branch-only, freestanding work); FLIP flag enable_eqsat_scalar_canonicalize DEFAULT to TRUE and KEEP it (commit 55b87ea4be); run a LARGE slt corpus locally with the flag on; regenerate the EXPLAIN goldens that legitimately change (result-based slt should be unchanged since the canonicalizer is semantics-preserving); assess for regressions before rewriting.
    - CORPUS SURVEY #1 (transform/ + explain/, 77 files, 2012 queries, flag-on default): only 3 output-failures, success=2009. Tiny blast radius (the flag only swaps reduce->eqsat INSIDE canonicalize_predicates, so non-predicate simplifications still go through reduce elsewhere). The 3 failures are all in explain/broken_statements.slt: `EXPLAIN ... BROKEN SELECT mz_unsafe.mz_panic('forced optimizer panic')`. With the flag on, mz_panic is EVALUATED during optimization (panic at func/impls/string.rs:1253 via FoldConstants) -> "forced optimizer panic" instead of the expected BROKEN "stage not present" error. reduce DELIBERATELY excludes Panic from const-folding (reduce/unary.rs:26 `*func != UnaryFunc::Panic`); eqsat const_fold has NO such guard. NOTE the immediate backtrace is FoldConstants not eqsat const_fold, so the mechanism needs confirming (does the eqsat path expose the Panic to FoldConstants? does it affect NORMAL SELECT mz_panic queries = serious optimizer-crash bug, or only the BROKEN debug path = benign golden change?). Inventory of remaining reduce-vs-eqsat gaps: scalar-task... reduce-vs-eqsat-rule-inventory.md (boolean layer COMPLETE; HIGH-impact MISSING = binary identity elim + Coalesce simplification, but they rarely bite predicates since reduce still runs elsewhere). FIX DONE (agent fix-panic-fold, commit c65a0527b6; report scalar-task-3c-panic-report.md). SEVERITY = SERIOUS: a NORMAL predicate query `WHERE mz_unsafe.mz_panic('boom')='x'` aborts the optimizer with the flag on (eqsat const_fold evals the literal-arg Panic during saturation -> panic caught by catch_unwind_optimize -> query fails to optimize, instead of planning it to panic at RUNTIME as reduce does). ROOT CAUSE: const_fold had no Panic exclusion; reduce excludes it (reduce/unary.rs:26). FIX: const_fold skips folding CallUnary{Panic,..} (mirrors reduce). MISATTRIBUTION CORRECTED: broken_statements.slt is projection-only (canonicalizer never sees it; its panic fires via FoldConstants on a Map, caught by BROKEN -> committed message); it passes flag-on 14/14. The 3 survey failures were the PREDICATE-mz_panic files (predicate_pushdown / cardinality_estimate_panic). New tests: test_no_fold_panic (unit) + predicate_reduction.slt EXPLAIN regression (WHERE mz_panic='x' stays PLANNED not evaluated). 88 eqsat tests + differential pass; clippy+fmt clean. NOTE (out of scope, matches reduce): isnull_fold folds IsNull(Panic(lit))->false, eliding a would-be runtime panic. Re-running transform+explain survey with the fix to confirm 0 failures.
    - CORPUS SURVEY #2 (per-file, fresh server each = CI-accurate; the combined-invocation run had cross-file state artifacts, ignore it). After the Panic fix: only 2 files fail, 1 query each, both PLAN diffs (no crashes): (1) transform/join_index.slt: `where nullif(foo.a,0)=-bar.a` -> eqsat leaves a DUPLICATE predicate `(#2) IS NOT NULL AND (#2) IS NOT NULL` that reduce dedups (quality regression, semantically equal); (2) transform/literal_constraints.slt: two top-level AND conjuncts SWAPPED vs golden (benign operand-order-only). FIX DONE (agent fix-dup-pred, commit efe2a4c0a3; report scalar-task-3c-dup-report.md). ROOT CAUSE of the dup: PredicatePushdown emits NOT(IsNull(#2)) and NOT(IsNull(NegInt32(#2))); reduce's decompose_is_null converges IsNull(-x)->IsNull(x) then dedups, but eqsat deliberately does NOT (NegInt32 overflow error -> exact-eval contract; decompose_is_null is deferred), so the two distinct forms reach ReduceScalars which converges them via plain reduce but did not dedup its Filter predicate list. FIX: ReduceScalars Filter branch order-preserving dedup (BTreeSet+retain, no sort; sound P AND P=P; flag-off no-op since reduce dedups upstream). join_index now MATCHES the committed golden (dup gone, no golden change needed); literal_constraints benign reorder regenerated (1 line). 288 mz-transform tests + differential pass; clippy/fmt clean. Accepted (proportionate light review: trivially-sound dedup, well-documented, flag-off no-op verified by suite). transform/+explain/ are now FULLY CLEAN flag-on.
    - CORPUS SURVEY #3 (list-form, faster per user tip; flag-on default): top-level(266)+postgres(15)+introspection(6)+attributes(3)+autogenerated(5) = ~295 files. Found 4 list-form failures, triaged INDIVIDUALLY (fresh server = authoritative):
      * catalog_server_explain.slt: 17 list-failures but PASSES 289/289 ALONE -> list-mode cross-file artifact, NOT real.
      * postgres/timestamp.slt: bail -> PRE-EXISTING UNRELATED (db error invalid input syntax for type timestamp 'Mon Feb 10 17:32:01 1997 PST' on INSERT, pre-optimization; nothing to do with eqsat).
      * ldbc_bi_physical.slt + ldbc_bi_eager_physical.slt: 1 EXPLAIN PHYSICAL diff each, BENIGN (`#2{w}=#2{right_col0_0}` -> `#2{w}=#2{w}`: Column(2) hash-conses ignoring the TreatAsEqual display-name, so name-variants of the same index merge; semantically #2=#2 identical, only the EXPLAIN label changes). REGENERATED + committed.
      NET: across the whole corpus, eqsat-caused diffs are tiny and all benign-or-fixed (join_index dup FIXED; literal_constraints reorder regen; 2 ldbc name-normalizations regen). The canonicalizer is essentially behavior-neutral with the flag on; result tests pass. NOTE list-form has cross-file artifacts (catalog_server_explain) -> confirm failures per-file.
    - CORPUS SURVEY #4 DONE: cockroach(101) + advent-of-code/2023(25) upstream correctness dirs (2505 results). These have MANY pre-existing failures (upstream CockroachDB SQL Materialize only partially supports: parse-failure=85, plan-failure=341, output-failure=55, bail=18, etc.). Ran flag-ON vs flag-OFF (temporarily flipped default to false + rebuilt for the baseline, then restored true): aggregates are BYTE-IDENTICAL both ways (unsupported=5 parse=85 plan=341 unexpected-plan-success=26 wrong-col=1 output=55 bail=18 success=1974 total=2505). So the flag causes ZERO change in the upstream dirs; all failures are pre-existing and flag-independent.
    >>> PHASE 3c CORPUS VALIDATION COMPLETE. Full local slt corpus (all 499 files) surveyed flag-on. The scalar canonicalizer is BEHAVIOR-NEUTRAL with the flag on: result tests pass; the only flag-on golden changes were 3 benign cosmetic EXPLAIN diffs (literal_constraints AND-reorder; 2 ldbc column-name normalizations) all regenerated+committed, plus the join_index duplicate (fixed via ReduceScalars dedup, matches golden). Two real bugs caught+fixed by the corpus: serious mz_panic optimizer crash (const_fold Panic guard) and the duplicate-predicate quality regression. Non-issues: catalog_server_explain (list-mode cross-file artifact, passes alone), postgres/timestamp (pre-existing parse error, unrelated). Flag default is TRUE on this branch. <<<
    - FUTURE (user-confirmed 2026-06-27): after the current phases, extract a SHARED GENERIC e-graph core unifying the relational + scalar engines (the real integration). Memory: eqsat-shared-core-extraction. Deferred eqsat rules remain: decompose_is_null, subset factoring, nullability-analysis gate disjuncts, binary identity elim, Coalesce simplification, involution elim, If->AND/OR, If-distribution (see reduce-vs-eqsat-rule-inventory.md; none are soundness issues).
  * 3a (AND/OR associativity flattening, unconditional/no gate, mirrors reduce flatten_associative; delivers flat conjunction downstream needs). Brief scalar-task-3a-brief.md, report -3a-report.md, review -3a-review.md, fix-report -3a-fix-report.md. DONE (rule commit f7cfac08de + hardening commit a87d6e03b3). REVIEWED APPROVED (review-3a OPUS). Implementer surfaced+fixed a TERMINATION hazard: flatten_assoc + and_or_single create a direct self-loop (OR(c0,c0)->dedup->OR(c0)->single->union OR-node-of-c0 into c0's class) whose splice squares the operand vector (4^k) BELOW MAX_ENODES -> hang. Fix: one-hop guard (skip inner same-func node whose canon children contain the operand's canon) handles all cycles the current rules produce; review flagged a LATENT transitive/2-class cycle the one-hop guard misses (could not construct from current rules) -> defense-in-depth FLATTEN_MAX_OPERANDS=4096 cap (declines splice if exceeded, sound) + 2 deterministic cycle regression tests. 56 scalar rule tests / 276 mz-transform tests pass; differential passes. Minors (HashSet iteration order: sound, pre-existing) noted. 3A COMPLETE.
  * 2c: form-targeted extraction / cost objective (consumer catalog: temporal mz_now binary, literal-eq, filter chars, flat conjunction). LOAD-BEARING; design objective first. Verify CLU-137 repro -> top-level binary mz_now()<ts; goldens unchanged.
- Phase 3: wire into canonicalize.rs (flag-gated reduce replacement) + full slt/td. PENDING


## RESUME STATE (compaction 2026-06-26)
- Phase 0 IN FLIGHT: agent a250353f27e453428 (opus) building src/transform/src/eqsat/scalar/{mod,node,egraph,lower,raise}.rs + `pub mod scalar;` in eqsat.rs + identity round-trip test `raise(lower(e))==e` (NOT ==reduce; corrected from plan).
  ON RESUME: do NOT re-dispatch. Check that agent's completion (task id a250353f27e453428). If done: verify `cargo check -p mz-transform --tests` clean (stale-LSP diagnostics are common this session, trust cargo not LSP), confirm the round-trip test passes, generate review-package (BASE=db27303efe HEAD=<phase0 commit>), dispatch a reviewer, then mark Phase 0 complete and dispatch Phase 1.
- Phases 1-3 per plan .claude/plans/scalar_eqsat_canonicalizer.md, sequential, each its own implementer+reviewer (subagent-driven-development flow). Phase 1=~40 local rules+analyses(could_error/lit/arity)+per-rule property tests. Phase 2=undistribute factoring(ungated)/absorption(could_error-gated) split + form-targeted extraction (consumer catalog: temporal mz_now binary, literal-eq, filter chars, flat conjunction). Phase 3=flag enable_eqsat_scalar_canonicalize (mirror optimize.rs:155) + swap reduce at canonicalize.rs:230/437(+87) flag-gated + full slt/td + CLU-137 repro fixed.
- Verified refs: reduce@scalar.rs:689; canonicalize reduce@87,230,437; eqsat submodules@eqsat.rs:23-37; flag pattern@optimize.rs:155.

## SESSION ARC (for orientation post-compaction):
1. WMR version-lift experiment: MERGE-READY (5a94e4189f..0fab93ba7f), user chose merge-anyway despite redundancy. Final review clean.
2. Marginal-value harness (eqsat_arrangement_benchmark.rs): measured eqsat net arrangement value = 0. THREE relational levers CLOSED (key-choice structural-empty; JI-subsumption structural-neg; join-order strong-neg). Scalar = passthrough (eqsat opaque on scalars).
3. NOW: full scalar e-graph canonicalizer = the one live lever (qualitative value: non-destructive/no CLU-137 bug class + saturation-complete vs reduce). Phase 0 in flight.
All findings in memory: project_egraph_marginal_value_harness.md (+wmr_lift_experiment, +scalar_expressions).
