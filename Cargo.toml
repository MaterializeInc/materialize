[workspace]
members = [
    "fuzz",
    "src/ccsr",
    "src/comm",
    "src/dataflow",
    "src/dataflow-bin",
    "src/dataflow-types",
    "src/expr",
    "src/interchange",
    "src/metastore",
    "src/metrics",
    "src/materialize",
    "src/ore",
    "src/repr",
    "src/sql",
    "src/testdrive",
    "src/sqllogictest",
]

[profile.release]
debug = true

[patch.crates-io]
# TODO(benesch): remove rdkafka patch when our message queue nonempty callbacks
# patch lands upstream.
rdkafka = { git = "ssh://git@github.com/MaterializeInc/rust-rdkafka.git" }
avro-rs = { git = "ssh://git@github.com/MaterializeInc/avro-rs.git" }
futures = { git = "ssh://git@github.com/MaterializeInc/futures-rs.git", branch = "0.1" }

# This is a slightly confusing syntax because of the fact that ssh urls use `:`
# instead of `/` to separate the domain, so this serves as an example
#
# [patch."ssh://git@github.com/MaterializeInc/sqlparser.git"]
# sqlparser = { path = "../sqlparser" }
