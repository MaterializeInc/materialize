---
source: src/mz/src/bin/mz/command/config.rs
revision: 3ea5fbaaff
---

# mz (bin)::command::config

Clap argument parser and dispatcher for `mz config`, with `Get`, `List`, `Set`, and `Remove` subcommands.
Delegates implementation to `mz::command::config`.
