---
source: src/mz/src/bin/mz/command/region.rs
revision: adfa404693
---

# mz (bin)::command::region

Clap argument parser and dispatcher for `mz region`, with `Enable` and `List` subcommands (including options for version, extra args, and resource allocations).
Delegates implementation to `mz::command::region`.
