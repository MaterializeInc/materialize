---
source: src/mz/src/bin/mz/command/secret.rs
revision: f95dd0d16b
---

# mz (bin)::command::secret

Clap argument parser and dispatcher for `mz secret create`, accepting optional database, schema, secret name, and a `--force` flag for upsert behavior.
Delegates implementation to `mz::command::secret`.
