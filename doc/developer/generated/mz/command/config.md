---
source: src/mz/src/command/config.rs
revision: 3ea5fbaaff
---

# mz::command::config

Implements the `mz config` subcommand for reading and writing global configuration parameters (`profile`, `vault`) in the configuration file.
Provides `get`, `list`, `set`, and `remove` functions operating on a `Context`.
