- name: "syntax"
  code: |
    justify_days ( <interval> )
  syntax_elements:
    - name: "`<interval>`"
      description: |
        An [`interval`](/sql/types/interval/) value to justify. Returns a new interval such that 30-day time periods are converted to months.
