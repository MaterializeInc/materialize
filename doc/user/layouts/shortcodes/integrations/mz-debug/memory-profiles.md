### Memory profiles

`mz-debug` outputs heap profiles if
[`--dump-heap-profiles`](#dump-heap-profiles) is `true` (the default).
Each memory profile is stored under `profiles/{service}.memprof.pprof.gz`.
