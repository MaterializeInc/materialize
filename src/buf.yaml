# Copyright Materialize, Inc. and contributors. All rights reserved.
#
# Use of this software is governed by the Business Source License
# included in the LICENSE file at the root of this repository.
#
# As of the Change Date specified in that file, in accordance with
# the Business Source License, use of this software will be governed
# by the Apache License, Version 2.0.

# File generated by generate-buf-config.py - DO NOT EDIT

---
version: v1
breaking:
  use:
    - WIRE
  ignore:
    # reason: Ignore because plans are currently not persisted.
    - expr/src/scalar.proto
    # reason: we very carefully evolve these protobuf definitions
    - persist-client/src/internal/state.proto
lint:
  use:
    - DEFAULT
build:
  excludes:
    # external code
    - testdrive/ci/protobuf-bin
    - testdrive/ci/protobuf-include
    # Fuzz crates have their own `[workspace]` (required for nightly +
    # libFuzzer) so each maintains its own `target/`. Build-script deps
    # like `protobuf-native` extract `.proto` files into that tree —
    # don't scan it. This list is auto-generated from `src/*/fuzz` by
    # generate-buf-config.py, so new fuzz crates are covered automatically.
    - avro/fuzz/target
    - catalog-protos/fuzz/target
    - expr/fuzz/target
    - interchange/fuzz/target
    - mysql-util/fuzz/target
    - persist-client/fuzz/target
    - pgcopy/fuzz/target
    - pgrepr/fuzz/target
    - pgtz/fuzz/target
    - pgwire/fuzz/target
    - postgres-util/fuzz/target
    - repr/fuzz/target
    - sql-parser/fuzz/target
    - sql-server-util/fuzz/target
    - storage-types/fuzz/target
    - storage/fuzz/target
    - transform/fuzz/target
