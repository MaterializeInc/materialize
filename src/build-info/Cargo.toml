[package]
name = "build-info"
description = "Metadata about a Materialize build."
version = "0.0.0"
edition = "2021"
publish = false

[features]
default = ["semver"]

[dependencies]
semver = { version = "1.0.4", optional = true }
