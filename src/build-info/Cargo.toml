[package]
name = "mz-build-info"
description = "Metadata about a Materialize build."
version = "0.0.0"
edition = "2021"
rust-version = "1.58.0"
publish = false

[dependencies]
semver = { version = "1.0.6", optional = true }

[features]
default = ["semver"]
