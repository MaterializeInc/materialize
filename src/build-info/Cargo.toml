[package]
name = "build-info"
description = "Metadata about a Materialize build."
version = "0.0.0"
edition = "2021"
publish = false
rust-version = "1.58.0"

[features]
default = ["semver"]

[dependencies]
semver = { version = "1.0.5", optional = true }
