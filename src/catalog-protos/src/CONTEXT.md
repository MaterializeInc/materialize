# mz_catalog_protos/src

Source root of the `mz-catalog-protos` crate. Cargo-conventional location;
no module boundary distinct from the crate itself.

See [`../CONTEXT.md`](../CONTEXT.md) for the crate's role, the durable-schema
seam, the frozen-snapshot upgrade chain, and the MD5-guarded immutability
invariant.
