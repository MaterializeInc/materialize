[package]
name = "mz-compute"
description = "The compute layer of Materialize Platform."
version = "0.0.0"
edition = "2021"
publish = false
rust-version = "1.58.0"

[dependencies]

[dev-dependencies]
