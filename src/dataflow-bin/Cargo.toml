[package]
name = "dataflow-bin"
description = "Utility binaries for the dataflow crate."
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
getopts = "0.2"
timely = { git = "https://github.com/benesch/timely-dataflow", features = ["bincode"], branch = "crossbeam-channels" }
