[package]
name = "metabase"
description = "An API client for Metabase."
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
reqwest = "0.11.1"
serde = { version = "1.0.123", features = ["derive"] }
