[package]
name = "metabase"
description = "An API client for Metabase."
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
reqwest = "0.10.8"
serde = { version = "1.0.0", features = ["derive"] }
