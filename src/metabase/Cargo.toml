[package]
name = "metabase"
description = "An API client for Metabase."
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
reqwest = "0.11.7"
serde = { version = "1.0.130", features = ["derive"] }
