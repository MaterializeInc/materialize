[package]
name = "pid-file"
description = "PID file management."
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
libc = "0.2.103"
ore = { path = "../ore", default-features = false }

[dev-dependencies]
tempfile = "3.2.0"

[build-dependencies]
cc = "1.0.71"
