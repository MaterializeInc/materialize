[package]
name = "mz-pid-file"
description = "PID file management."
version = "0.0.0"
edition = "2021"
rust-version = "1.60.0"
publish = false

[dependencies]
libc = "0.2.125"
mz-ore = { path = "../ore", default-features = false }

[dev-dependencies]
tempfile = "3.2.0"

[build-dependencies]
cc = "1.0.73"
