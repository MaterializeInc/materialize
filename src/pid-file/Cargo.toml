[package]
name = "pid-file"
description = "PID file management."
version = "0.0.0"
edition = "2021"
publish = false
rust-version = "1.58.0"

[dependencies]
libc = "0.2.117"
ore = { path = "../ore", default-features = false }

[dev-dependencies]
tempfile = "3.2.0"

[build-dependencies]
cc = "1.0.72"
