[package]
name = "repr_test_util"
description = "Utilities for creating objects in the repr crate for testing"
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
lazy_static = "1.4.0"
lowertest = {path = "../lowertest"}
repr = {path = "../repr"}
proc-macro2 = "1.0.27"

[dev-dependencies]
datadriven = "0.5.0"
