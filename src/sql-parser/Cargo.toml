[package]
name = "sql-parser"
description = "The lexer and parser for Materialize's SQL dialect."
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
failure = "0.1.8"
log = "0.4.5"
ore = { path = "../ore" }
repr = { path = "../repr" }
lazy_static = "1.4.0"

[dev-dependencies]
datadriven = "0.1.0"
matches = "0.1"

[build-dependencies]
anyhow = "1.0.31"
walkabout = { path = "../walkabout" }
