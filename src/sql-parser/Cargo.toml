[package]
name = "sql-parser"
description = "The lexer and parser for Materialize's SQL dialect."
version = "0.1.0"
edition = "2018"

[dependencies]
failure = "0.1.6"
log = "0.4.5"

[dev-dependencies]
matches = "0.1"
