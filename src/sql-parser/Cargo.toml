[package]
name = "sql-parser"
description = "The lexer and parser for Materialize's SQL dialect."
version = "0.1.0"
edition = "2018"
publish = false
build = "build/main.rs"

[dependencies]
failure = "0.1.8"
log = "0.4.5"
repr = { path = "../repr" }
lazy_static = "1.4.0"
datadriven = "0.1.0"

[dev-dependencies]
matches = "0.1"

[build-dependencies]
anyhow = "1.0.31"
proc-macro2 = "1.0.18"
quote = "1.0.6"
syn = "1.0.30"
