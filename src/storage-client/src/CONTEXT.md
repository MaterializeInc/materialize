# mz_storage_client/src

Source root of `mz-storage-client`. Cargo-conventional location.

See [`../CONTEXT.md`](../CONTEXT.md) for the crate's role and the
`StorageCollectionsImpl` (3,342 LOC, in client crate but instantiated by
combined `mz-controller`) inversion.
