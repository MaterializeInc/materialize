[package]
name = "transform"
description = "Transformations of Materialize expressions."
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
dataflow-types = { path = "../dataflow-types" }
expr = { path = "../expr" }
itertools = "0.10.0"
repr = { path = "../repr" }

[dev-dependencies]
anyhow = "1.0.38"
datadriven = "0.5.0"
