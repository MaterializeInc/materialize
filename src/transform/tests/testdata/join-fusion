# Copyright Materialize, Inc. and contributors. All rights reserved.
#
# Use of this software is governed by the Business Source License
# included in the LICENSE file.
#
# As of the Change Date specified in that file, in accordance with
# the Business Source License, use of this software will be governed
# by the Apache License, Version 2.0.

cat
(defsource x [int64 int64])
----
ok

cat
(defsource y [int64 int64])
----
ok

build
(join
  [(get x)
   (filter
     (join [(get x) (get y)] [[#0 #2]])
     [#1])]
   [[#0 #2]])
----
error: Object spec [Ident { sym: get, span: bytes(2027..2030) }, Ident { sym: x, span: bytes(2031..2032) }] (type Vec<MirScalarExpr>) has unsupported delimiter Parenthesis

build apply=JoinFusion
(join
  [(get x)
   (filter
     (join [(get x) (get y)] [[#0 #2]])
     [#1])]
   [[#0 #2]])
----
error: Object spec [Ident { sym: get, span: bytes(2122..2125) }, Ident { sym: x, span: bytes(2126..2127) }] (type Vec<MirScalarExpr>) has unsupported delimiter Parenthesis

build apply=JoinFusion
(join
  [(get x)
   (filter
     (get y)
     [#1])]
   [[#0 #2]])
----
error: Object spec [Ident { sym: get, span: bytes(2217..2220) }, Ident { sym: x, span: bytes(2221..2222) }] (type Vec<MirScalarExpr>) has unsupported delimiter Parenthesis

# Check that filters around non-join operators are handled properly
build apply=JoinFusion
(join
  [(get x)
   (filter
     (join [(get x) (get y)] [[#0 #2]])
     [#1])
   (filter
     (get y)
     [#1])]
   [[#0 #2 #6]])
----
error: Object spec [Ident { sym: get, span: bytes(2285..2288) }, Ident { sym: x, span: bytes(2289..2290) }] (type Vec<MirScalarExpr>) has unsupported delimiter Parenthesis

build apply=(JoinFusion,PredicatePushdown)
(join
  [(get x)
   (filter
     (join [(get x) (get y)] [[#0 #2]])
     [#1])
   (filter
     (get y)
     [#1])]
   [[#0 #2 #6]])
----
error: Object spec [Ident { sym: get, span: bytes(2418..2421) }, Ident { sym: x, span: bytes(2422..2423) }] (type Vec<MirScalarExpr>) has unsupported delimiter Parenthesis
