[package]
name = "walkabout"
description = "AST visitor generation."
version = "0.1.0"
edition = "2018"
publish = false

[dependencies]
anyhow = "1.0.37"
fstrings = "0.2.3"
ore = { path = "../ore" }
quote = "1.0.8"
syn = { version = "1.0.55", features = ["extra-traits", "full", "parsing"] }

[dev-dependencies]
datadriven = "0.4.0"
tempfile = "3.1.0"
