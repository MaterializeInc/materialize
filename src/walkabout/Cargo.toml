[package]
name = "walkabout"
description = "AST visitor generation."
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
anyhow = "1.0.38"
fstrings = "0.2.3"
itertools = "0.9.0"
ore = { path = "../ore" }
quote = "1.0.9"
syn = { version = "1.0.55", features = ["extra-traits", "full", "parsing"] }

[dev-dependencies]
datadriven = "0.5.0"
tempfile = "3.2.0"
