# Copyright Materialize, Inc. and contributors. All rights reserved.
#
# Use of this software is governed by the Business Source License
# included in the LICENSE file at the root of this repository.
#
# As of the Change Date specified in that file, in accordance with
# the Business Source License, use of this software will be governed
# by the Apache License, Version 2.0.

statement ok
CREATE TABLE t (
  a int,
  b text,
  c date
)

statement ok
CREATE TABLE u (
  d int
)

statement ok
CREATE TABLE v (
  e double,
  f bool
)

#### NULL propagation ####

# Union that does not propagate a null
query T multiline
EXPLAIN OPTIMIZED PLAN WITH(types, humanized expressions) AS VERBOSE TEXT FOR (SELECT * FROM v WHERE f) UNION ALL (SELECT * FROM v WHERE e IS NOT NULL)
----
Explained Query:
  Union // { types: "(float64?, rbool?)" }
    Filter #1{f} // { types: "(float64?, rbool)" }
      ReadStorage materialize.public.v // { types: "(float64?, rbool?)" }
    Filter (#0{e}) IS NOT NULL // { types: "(float64, rbool?)" }
      ReadStorage materialize.public.v // { types: "(float64?, rbool?)" }

Source materialize.public.v

Target cluster: quickstart

EOF

# Union that does propagate a null
query T multiline
EXPLAIN OPTIMIZED PLAN WITH(types, humanized expressions) AS VERBOSE TEXT FOR (SELECT * FROM v WHERE e = 1.25) UNION ALL (SELECT * FROM v WHERE e IS NOT NULL)
----
Explained Query:
  Union // { types: "(float64, rbool?)" }
    Filter (#0{e} = 1.25) // { types: "(float64, rbool?)" }
      ReadStorage materialize.public.v // { types: "(float64?, rbool?)" }
    Filter (#0{e}) IS NOT NULL // { types: "(float64, rbool?)" }
      ReadStorage materialize.public.v // { types: "(float64?, rbool?)" }

Source materialize.public.v

Target cluster: quickstart

EOF

# Constant + reduce
# Nullability should be correctly inferred on the Constant
query T multiline
EXPLAIN OPTIMIZED PLAN WITH(types, humanized expressions) AS VERBOSE TEXT FOR
(SELECT 1 as a, 'hello' as b, null::date as c)
EXCEPT
(SELECT sum(a) as a, max(b) as b, c FROM t GROUP BY c)
----
Explained Query:
  Threshold // { types: "(rint64?, rstring?, rdate?)" }
    Union // { types: "(rint64?, rstring?, rdate?)" }
      Negate // { types: "(rint64?, rstring?, rdate?)" }
        Project (#3{sum_a}, #1{max_b}, #0{c}) // { types: "(rint64?, rstring?, rdate?)" }
          Join on=(#0{c} = #2{c}) type=differential // { types: "(rdate?, rstring?, rdate?, rint64?)" }
            ArrangeBy keys=[[#0{c}]] // { types: "(rdate?, rstring?)" }
              Reduce group_by=[#1{c}] aggregates=[max(#0{b})] // { types: "(rdate?, rstring?)" }
                Project (#1{b}, #2{c}) // { types: "(rstring?, rdate?)" }
                  ReadStorage materialize.public.t // { types: "(rint32?, rstring?, rdate?)" }
            ArrangeBy keys=[[#0{c}]] // { types: "(rdate?, rint64?)" }
              Reduce group_by=[#1{c}] aggregates=[sum(#0{a})] // { types: "(rdate?, rint64?)" }
                Project (#0{a}, #2{c}) // { types: "(rint32?, rdate?)" }
                  ReadStorage materialize.public.t // { types: "(rint32?, rstring?, rdate?)" }
      Constant // { types: "(rint64, rstring, rdate?)" }
        - (1, "hello", null)

Source materialize.public.t

Target cluster: quickstart

EOF

#### Correct column scalar type ####

query T multiline
EXPLAIN OPTIMIZED PLAN WITH(types, humanized expressions) AS VERBOSE TEXT FOR
SELECT t.* FROM u LEFT OUTER JOIN t on t.a = u.d
----
Explained Query:
  With
    cte l0 =
      ArrangeBy keys=[[#0{d}]] // { types: "(rint32)" }
        Filter (#0{d}) IS NOT NULL // { types: "(rint32)" }
          ReadStorage materialize.public.u // { types: "(rint32?)" }
    cte l1 =
      Project (#0{d}, #2{b}, #3{c}) // { types: "(rint32, rstring?, rdate?)" }
        Join on=(#0{d} = #1{a}) type=differential // { types: "(rint32, rint32, rstring?, rdate?)" }
          Get l0 // { types: "(rint32)" }
          ArrangeBy keys=[[#0{a}]] // { types: "(rint32, rstring?, rdate?)" }
            Filter (#0{a}) IS NOT NULL // { types: "(rint32, rstring?, rdate?)" }
              ReadStorage materialize.public.t // { types: "(rint32?, rstring?, rdate?)" }
  Return // { types: "(rint32?, rstring?, rdate?)" }
    Union // { types: "(rint32?, rstring?, rdate?)" }
      Map (null, null, null) // { types: "(rint32?, rstring?, rdate?)" }
        Union // { types: "()" }
          Negate // { types: "()" }
            Project () // { types: "()" }
              Join on=(#0{d} = #1{d}) type=differential // { types: "(rint32, rint32)" }
                Get l0 // { types: "(rint32)" }
                ArrangeBy keys=[[#0{d}]] // { types: "(rint32)" }
                  Distinct project=[#0{d}] // { types: "(rint32)" }
                    Project (#0{d}) // { types: "(rint32)" }
                      Get l1 // { types: "(rint32, rstring?, rdate?)" }
          Project () // { types: "()" }
            ReadStorage materialize.public.u // { types: "(rint32?)" }
      Get l1 // { types: "(rint32, rstring?, rdate?)" }

Source materialize.public.t
  filter=((#0{a}) IS NOT NULL)
Source materialize.public.u

Target cluster: quickstart

EOF

query T multiline
EXPLAIN OPTIMIZED PLAN WITH(types, humanized expressions) AS VERBOSE TEXT FOR
(SELECT null::boolean as f1, 10 as f2) EXCEPT (SELECT min(f), count(*) FROM v WHERE (select d::double FROM u) = v.e GROUP BY e LIMIT 1)
----
Explained Query:
  With
    cte l0 =
      Project (#0{e}, #1{f}) // { types: "(float64, rbool?)" }
        Join on=(#0{e} = #2) type=differential // { types: "(float64, rbool?, float64)" }
          ArrangeBy keys=[[#0{e}]] // { types: "(float64, rbool?)" }
            Filter (#0{e}) IS NOT NULL // { types: "(float64, rbool?)" }
              ReadStorage materialize.public.v // { types: "(float64?, rbool?)" }
          ArrangeBy keys=[[#0]] // { types: "(float64?)" }
            Union // { types: "(float64?)" }
              Project (#1) // { types: "(float64?)" }
                Filter (#0{d}) IS NOT NULL // { types: "(rint32, float64?)" }
                  Map (integer_to_double(#0{d})) // { types: "(rint32?, float64?)" }
                    ReadStorage materialize.public.u // { types: "(rint32?)" }
              Project (#1) // { types: "(float64)" }
                FlatMap guard_subquery_size(#0{count}) // { types: "(rint64, float64)" }
                  Reduce aggregates=[count(*)] // { types: "(rint64)" }
                    Project () // { types: "()" }
                      ReadStorage materialize.public.u // { types: "(rint32?)" }
  Return // { types: "(rbool?, rint64)" }
    Threshold // { types: "(rbool?, rint64)" }
      Union // { types: "(rbool?, rint64)" }
        Negate // { types: "(rbool?, rint64)" }
          TopK limit=1 // { types: "(rbool?, rint64)" }
            Project (#1{min_f}, #3{count}) // { types: "(rbool?, rint64)" }
              Join on=(#0{e} = #2{e}) type=differential // { types: "(float64, rbool?, float64, rint64)" }
                ArrangeBy keys=[[#0{e}]] // { types: "(float64, rbool?)" }
                  Reduce group_by=[#0{e}] aggregates=[min(#1{f})] // { types: "(float64, rbool?)" }
                    Get l0 // { types: "(float64, rbool?)" }
                ArrangeBy keys=[[#0{e}]] // { types: "(float64, rint64)" }
                  Reduce group_by=[#0{e}] aggregates=[count(*)] // { types: "(float64, rint64)" }
                    Project (#0{e}) // { types: "(float64)" }
                      Get l0 // { types: "(float64, rbool?)" }
        Constant // { types: "(rbool?, rint64)" }
          - (null, 10)

Source materialize.public.u
Source materialize.public.v
  filter=((#0{e}) IS NOT NULL)

Target cluster: quickstart

EOF
